const name="Himanshu";
const repoCount=69;

console.log(`The repo count is ${repoCount} and the owner of the repos is ${name}`);

const fullName= new String("Himanshu Mishra");

//charAt, indexOf, substring(start index, end index(not included)) (start index if less than 0 then it will by default be 0)
//splice(start, end);

console.log(fullName.slice(-7,10));